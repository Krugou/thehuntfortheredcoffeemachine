{
	"type": "module",
	"name": "thehuntfortheredcoffeemachine",
	"version": "1.0.0",
	"scripts": {
		"start": "npm run vrdev",
		"dev": "npm i && vite -d --port 3333 --open --host",
		"vrdev": "cross-env USE_SSL=true npm run dev",
		"build": "npm i && vite build --emptyOutDir"
	},
	"dependencies": {
		"rollup-plugin-terser": "^7.0.2",
		"three": "^0.162.0"
	},
	"devDependencies": {
		"@vitejs/plugin-basic-ssl": "^1.1.0",
		"cross-env": "^7.0.3",
		"vite": "^5.2.10"
	}
}
